# CLI Tools

Standalone scripts that can be invoked by agents or run independently.

Each tool should:
- Work as a standalone CLI script
- Accept arguments for configuration
- Return structured output (JSON preferred for agent consumption)
