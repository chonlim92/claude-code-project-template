# MCP Servers

Model Context Protocol server implementations that expose tools to agents.

Each MCP server should:
- Have clear tool descriptions for agent discovery
- Handle errors gracefully and return meaningful messages
- Be independently testable
